<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- context.cacheDir — camera captures go here -->
    <cache-path name="cache" path="." />

    <!-- context.filesDir — evidence files stored in internal private storage -->
    <files-path name="internal" path="." />

    <!-- context.getExternalFilesDir() — app-scoped external storage only -->
    <external-files-path name="external_files" path="." />
</paths>
